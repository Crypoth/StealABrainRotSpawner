-- Script Status UI for Roblox
-- Created by XwareEXE

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Clean up any existing UI
if playerGui:FindFirstChild("ScriptStatusUI") then
    playerGui.ScriptStatusUI:Destroy()
end

-- Create main ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ScriptStatusUI"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- Create background particles container
local particlesFrame = Instance.new("Frame")
particlesFrame.Name = "ParticlesFrame"
particlesFrame.Size = UDim2.new(1, 0, 1, 0)
particlesFrame.Position = UDim2.new(0, 0, 0, 0)
particlesFrame.BackgroundTransparency = 1
particlesFrame.ZIndex = 1
particlesFrame.Parent = screenGui

-- Create main container
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 500, 0, 450)
mainFrame.Position = UDim2.new(0.5, -250, 0.5, -225)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
mainFrame.BackgroundTransparency = 0.05
mainFrame.BorderSizePixel = 0
mainFrame.ZIndex = 10
mainFrame.Parent = screenGui

-- Add corner rounding and border
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 15)
corner.Parent = mainFrame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(255, 0, 0)
stroke.Thickness = 2
stroke.Parent = mainFrame

-- Create header
local headerFrame = Instance.new("Frame")
headerFrame.Name = "HeaderFrame"
headerFrame.Size = UDim2.new(1, -20, 0, 50)
headerFrame.Position = UDim2.new(0, 10, 0, 10)
headerFrame.BackgroundTransparency = 1
headerFrame.Parent = mainFrame

-- Status indicator
local statusIndicator = Instance.new("Frame")
statusIndicator.Name = "StatusIndicator"
statusIndicator.Size = UDim2.new(0, 12, 0, 12)
statusIndicator.Position = UDim2.new(0, 0, 0, 19)
statusIndicator.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
statusIndicator.BorderSizePixel = 0
statusIndicator.Parent = headerFrame

local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(1, 0)
statusCorner.Parent = statusIndicator

-- Window title
local windowTitle = Instance.new("TextLabel")
windowTitle.Name = "WindowTitle"
windowTitle.Size = UDim2.new(0, 200, 0, 50)
windowTitle.Position = UDim2.new(0, 20, 0, 0)
windowTitle.BackgroundTransparency = 1
windowTitle.Text = "SCRIPT STATUS"
windowTitle.TextColor3 = Color3.fromRGB(255, 0, 0)
windowTitle.TextSize = 12
windowTitle.Font = Enum.Font.Code
windowTitle.TextXAlignment = Enum.TextXAlignment.Left
windowTitle.Parent = headerFrame

-- Window controls
local controlsFrame = Instance.new("Frame")
controlsFrame.Name = "ControlsFrame"
controlsFrame.Size = UDim2.new(0, 60, 0, 25)
controlsFrame.Position = UDim2.new(1, -70, 0, 12)
controlsFrame.BackgroundTransparency = 1
controlsFrame.Parent = headerFrame

-- Minimize button
local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Name = "MinimizeBtn"
minimizeBtn.Size = UDim2.new(0, 20, 0, 20)
minimizeBtn.Position = UDim2.new(0, 0, 0, 0)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(255, 170, 0)
minimizeBtn.Text = ""
minimizeBtn.BorderSizePixel = 0
minimizeBtn.Parent = controlsFrame

local minimizeCorner = Instance.new("UICorner")
minimizeCorner.CornerRadius = UDim.new(1, 0)
minimizeCorner.Parent = minimizeBtn

-- Close button
local closeBtn = Instance.new("TextButton")
closeBtn.Name = "CloseBtn"
closeBtn.Size = UDim2.new(0, 20, 0, 20)
closeBtn.Position = UDim2.new(0, 30, 0, 0)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
closeBtn.Text = ""
closeBtn.BorderSizePixel = 0
closeBtn.Parent = controlsFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(1, 0)
closeCorner.Parent = closeBtn

-- Main title
local mainTitle = Instance.new("TextLabel")
mainTitle.Name = "MainTitle"
mainTitle.Size = UDim2.new(1, -20, 0, 40)
mainTitle.Position = UDim2.new(0, 10, 0, 70)
mainTitle.BackgroundTransparency = 1
mainTitle.Text = "SPAWNER IS CURRENTLY DOWN"
mainTitle.TextColor3 = Color3.fromRGB(255, 0, 0)
mainTitle.TextSize = 22
mainTitle.Font = Enum.Font.Code
mainTitle.TextXAlignment = Enum.TextXAlignment.Center
mainTitle.Parent = mainFrame

-- Subtitle
local subtitle = Instance.new("TextLabel")
subtitle.Name = "Subtitle"
subtitle.Size = UDim2.new(1, -40, 0, 60)
subtitle.Position = UDim2.new(0, 20, 0, 120)
subtitle.BackgroundTransparency = 1
subtitle.Text = "The script is currently down due to steal a brainrot updating and changing their paths, trax is also down rn.."
subtitle.TextColor3 = Color3.fromRGB(204, 204, 204)
subtitle.TextSize = 14
subtitle.Font = Enum.Font.Code
subtitle.TextXAlignment = Enum.TextXAlignment.Center
subtitle.TextWrapped = true
subtitle.Parent = mainFrame

-- Estimated time section
local timeLabel = Instance.new("TextLabel")
timeLabel.Name = "TimeLabel"
timeLabel.Size = UDim2.new(1, -40, 0, 25)
timeLabel.Position = UDim2.new(0, 20, 0, 190)
timeLabel.BackgroundTransparency = 1
timeLabel.Text = "ESTIMATED TIME FOR FIX:"
timeLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
timeLabel.TextSize = 12
timeLabel.Font = Enum.Font.Code
timeLabel.TextXAlignment = Enum.TextXAlignment.Left
timeLabel.Parent = mainFrame

local timeContent = Instance.new("TextLabel")
timeContent.Name = "TimeContent"
timeContent.Size = UDim2.new(1, -40, 0, 25)
timeContent.Position = UDim2.new(0, 20, 0, 215)
timeContent.BackgroundTransparency = 1
timeContent.Text = "3-5 days"
timeContent.TextColor3 = Color3.fromRGB(255, 255, 255)
timeContent.TextSize = 16
timeContent.Font = Enum.Font.Code
timeContent.TextXAlignment = Enum.TextXAlignment.Left
timeContent.Parent = mainFrame

-- Discord section
local discordFrame = Instance.new("Frame")
discordFrame.Name = "DiscordFrame"
discordFrame.Size = UDim2.new(1, -40, 0, 100)
discordFrame.Position = UDim2.new(0, 20, 0, 250)
discordFrame.BackgroundColor3 = Color3.fromRGB(25, 0, 0)
discordFrame.BackgroundTransparency = 0.3
discordFrame.BorderSizePixel = 0
discordFrame.Parent = mainFrame

local discordCorner = Instance.new("UICorner")
discordCorner.CornerRadius = UDim.new(0, 10)
discordCorner.Parent = discordFrame

local discordStroke = Instance.new("UIStroke")
discordStroke.Color = Color3.fromRGB(255, 0, 0)
discordStroke.Thickness = 1
discordStroke.Transparency = 0.7
discordStroke.Parent = discordFrame

-- Discord label
local discordLabel = Instance.new("TextLabel")
discordLabel.Name = "DiscordLabel"
discordLabel.Size = UDim2.new(1, -20, 0, 25)
discordLabel.Position = UDim2.new(0, 10, 0, 5)
discordLabel.BackgroundTransparency = 1
discordLabel.Text = "JOIN OUR DISCORD FOR UPDATES:"
discordLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
discordLabel.TextSize = 12
discordLabel.Font = Enum.Font.Code
discordLabel.TextXAlignment = Enum.TextXAlignment.Left
discordLabel.Parent = discordFrame

-- Discord link display
local discordLink = Instance.new("TextLabel")
discordLink.Name = "DiscordLink"
discordLink.Size = UDim2.new(1, -20, 0, 25)
discordLink.Position = UDim2.new(0, 10, 0, 30)
discordLink.BackgroundTransparency = 1
discordLink.Text = "🎮 https://discord.gg/UMVSe5XfGC"
discordLink.TextColor3 = Color3.fromRGB(255, 255, 255)
discordLink.TextSize = 14
discordLink.Font = Enum.Font.Code
discordLink.TextXAlignment = Enum.TextXAlignment.Left
discordLink.Parent = discordFrame

-- Copy Discord button
local copyDiscordBtn = Instance.new("TextButton")
copyDiscordBtn.Name = "CopyDiscordBtn"
copyDiscordBtn.Size = UDim2.new(1, -20, 0, 30)
copyDiscordBtn.Position = UDim2.new(0, 10, 0, 60)
copyDiscordBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
copyDiscordBtn.Text = "COPY DISCORD LINK"
copyDiscordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
copyDiscordBtn.TextSize = 12
copyDiscordBtn.Font = Enum.Font.Code
copyDiscordBtn.BorderSizePixel = 0
copyDiscordBtn.Parent = discordFrame

local copyDiscordCorner = Instance.new("UICorner")
copyDiscordCorner.CornerRadius = UDim.new(0, 8)
copyDiscordCorner.Parent = copyDiscordBtn



-- Signature
local signature = Instance.new("TextLabel")
signature.Name = "Signature"
signature.Size = UDim2.new(1, -40, 0, 30)
signature.Position = UDim2.new(0, 20, 0, 365)
signature.BackgroundTransparency = 1
signature.Text = "Yours Sincerely, XwareEXE"
signature.TextColor3 = Color3.fromRGB(153, 153, 153)
signature.TextSize = 12
signature.Font = Enum.Font.Code
signature.TextXAlignment = Enum.TextXAlignment.Center
signature.Parent = mainFrame

-- Notification system
local notificationFrame = Instance.new("Frame")
notificationFrame.Name = "NotificationFrame"
notificationFrame.Size = UDim2.new(0, 300, 0, 60)
notificationFrame.Position = UDim2.new(1, -320, 0, 20)
notificationFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
notificationFrame.BackgroundTransparency = 0.1
notificationFrame.BorderSizePixel = 0
notificationFrame.Visible = false
notificationFrame.ZIndex = 100
notificationFrame.Parent = screenGui

local notifCorner = Instance.new("UICorner")
notifCorner.CornerRadius = UDim.new(0, 10)
notifCorner.Parent = notificationFrame

local notifStroke = Instance.new("UIStroke")
notifStroke.Color = Color3.fromRGB(255, 0, 0)
notifStroke.Thickness = 2
notifStroke.Parent = notificationFrame

local notificationText = Instance.new("TextLabel")
notificationText.Name = "NotificationText"
notificationText.Size = UDim2.new(1, -20, 1, -20)
notificationText.Position = UDim2.new(0, 10, 0, 10)
notificationText.BackgroundTransparency = 1
notificationText.Text = ""
notificationText.TextColor3 = Color3.fromRGB(255, 255, 255)
notificationText.TextSize = 14
notificationText.Font = Enum.Font.Code
notificationText.TextWrapped = true
notificationText.TextXAlignment = Enum.TextXAlignment.Center
notificationText.TextYAlignment = Enum.TextYAlignment.Center
notificationText.Parent = notificationFrame

-- Loading overlay
local loadingOverlay = Instance.new("Frame")
loadingOverlay.Name = "LoadingOverlay"
loadingOverlay.Size = UDim2.new(1, 0, 1, 0)
loadingOverlay.Position = UDim2.new(0, 0, 0, 0)
loadingOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
loadingOverlay.BackgroundTransparency = 0.2
loadingOverlay.BorderSizePixel = 0
loadingOverlay.Visible = false
loadingOverlay.ZIndex = 200
loadingOverlay.Parent = screenGui

-- Loading spinner
local loadingSpinner = Instance.new("Frame")
loadingSpinner.Name = "LoadingSpinner"
loadingSpinner.Size = UDim2.new(0, 50, 0, 50)
loadingSpinner.Position = UDim2.new(0.5, -25, 0.5, -25)
loadingSpinner.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
loadingSpinner.BackgroundTransparency = 0.7
loadingSpinner.BorderSizePixel = 0
loadingSpinner.Parent = loadingOverlay

local spinnerCorner = Instance.new("UICorner")
spinnerCorner.CornerRadius = UDim.new(1, 0)
spinnerCorner.Parent = loadingSpinner

local spinnerStroke = Instance.new("UIStroke")
spinnerStroke.Color = Color3.fromRGB(255, 0, 0)
spinnerStroke.Thickness = 4
spinnerStroke.Parent = loadingSpinner

-- Functions
local function showNotification(message, isSuccess)
    notificationText.Text = message
    if isSuccess then
        notifStroke.Color = Color3.fromRGB(0, 255, 0)
    else
        notifStroke.Color = Color3.fromRGB(255, 0, 0)
    end
    
    notificationFrame.Visible = true
    notificationFrame.Position = UDim2.new(1, 0, 0, 20)
    
    local slideIn = TweenService:Create(
        notificationFrame,
        TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Position = UDim2.new(1, -320, 0, 20)}
    )
    slideIn:Play()
    
    wait(3)
    
    local slideOut = TweenService:Create(
        notificationFrame,
        TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In),
        {Position = UDim2.new(1, 0, 0, 20)}
    )
    slideOut:Play()
    slideOut.Completed:Connect(function()
        notificationFrame.Visible = false
    end)
end

local function showLoading()
    loadingOverlay.Visible = true
    
    -- Spin animation
    local spinTween = TweenService:Create(
        loadingSpinner,
        TweenInfo.new(1, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1),
        {Rotation = 360}
    )
    spinTween:Play()
    
    wait(1.5)
    
    spinTween:Cancel()
    loadingOverlay.Visible = false
end

local function copyToClipboard(text)
    if setclipboard then
        setclipboard(text)
        return true
    else
        return false
    end
end

local function copyDiscordLink()
    local discordUrl = "https://discord.gg/UMVSe5XfGC"
    if copyToClipboard(discordUrl) then
        spawn(function()
            showNotification("Discord link copied to clipboard!", true)
        end)
    else
        spawn(function()
            showNotification("Clipboard not supported in this executor", false)
        end)
    end
end

local function copyAllInfo()
    local allInfo = [[SPAWNER IS CURRENTLY DOWN

The script is currently down due to steal a brainrot updating and changing their paths, trax is also down rn..

Estimated time for fix: 3-5 days

Please join our discord for any updates:
https://discord.gg/UMVSe5XfGC

Yours Sincerely,
XwareEXE]]

    if copyToClipboard(allInfo) then
        spawn(function()
            showNotification("All information copied to clipboard!", true)
        end)
    else
        spawn(function()
            showNotification("Clipboard not supported in this executor", false)
        end)
    end
end

local function minimizeWindow()
    spawn(function()
        showLoading()
        
        local minimizeTween = TweenService:Create(
            mainFrame,
            TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In),
            {Position = UDim2.new(0.5, -250, 1, 50)}
        )
        minimizeTween:Play()
        
        minimizeTween.Completed:Connect(function()
            mainFrame.Visible = false
            showNotification("Window minimized", false)
        end)
    end)
end

local function closeWindow()
    spawn(function()
        showLoading()
        
        local closeTween = TweenService:Create(
            mainFrame,
            TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In),
            {Size = UDim2.new(0, 0, 0, 0), Transparency = 1}
        )
        closeTween:Play()
        
        closeTween.Completed:Connect(function()
            screenGui:Destroy()
            showNotification("Script UI closed", false)
        end)
    end)
end

-- Create floating particles
local function createParticles()
    for i = 1, 30 do
        local particle = Instance.new("Frame")
        particle.Name = "Particle" .. i
        particle.Size = UDim2.new(0, 2, 0, 2)
        particle.Position = UDim2.new(math.random(), 0, 1, 10)
        particle.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        particle.BackgroundTransparency = 0.7
        particle.BorderSizePixel = 0
        particle.Parent = particlesFrame
        
        local particleCorner = Instance.new("UICorner")
        particleCorner.CornerRadius = UDim.new(1, 0)
        particleCorner.Parent = particle
        
        -- Animate particle
        spawn(function()
            while particle.Parent do
                local floatTween = TweenService:Create(
                    particle,
                    TweenInfo.new(math.random(4, 8), Enum.EasingStyle.Linear),
                    {Position = UDim2.new(math.random(), 0, -0.1, 0)}
                )
                floatTween:Play()
                floatTween.Completed:Wait()
                particle.Position = UDim2.new(math.random(), 0, 1, 10)
            end
        end)
    end
end

-- Status indicator pulse animation
spawn(function()
    while statusIndicator.Parent do
        local pulseTween = TweenService:Create(
            statusIndicator,
            TweenInfo.new(0.75, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {Size = UDim2.new(0, 15, 0, 15), BackgroundTransparency = 0.5}
        )
        pulseTween:Play()
        pulseTween.Completed:Wait()
        
        local pulseTween2 = TweenService:Create(
            statusIndicator,
            TweenInfo.new(0.75, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {Size = UDim2.new(0, 12, 0, 12), BackgroundTransparency = 0}
        )
        pulseTween2:Play()
        pulseTween2.Completed:Wait()
    end
end)

-- Glow animation for main frame
spawn(function()
    while mainFrame.Parent do
        local glowTween = TweenService:Create(
            stroke,
            TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {Transparency = 0.3}
        )
        glowTween:Play()
        glowTween.Completed:Wait()
        
        local glowTween2 = TweenService:Create(
            stroke,
            TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {Transparency = 0}
        )
        glowTween2:Play()
        glowTween2.Completed:Wait()
    end
end)

-- Button hover effects
local function addHoverEffect(button, hoverColor, normalColor)
    button.MouseEnter:Connect(function()
        local hoverTween = TweenService:Create(
            button,
            TweenInfo.new(0.2, Enum.EasingStyle.Quad),
            {BackgroundColor3 = hoverColor, Size = button.Size + UDim2.new(0, 2, 0, 2)}
        )
        hoverTween:Play()
    end)
    
    button.MouseLeave:Connect(function()
        local leaveTween = TweenService:Create(
            button,
            TweenInfo.new(0.2, Enum.EasingStyle.Quad),
            {BackgroundColor3 = normalColor, Size = button.Size - UDim2.new(0, 2, 0, 2)}
        )
        leaveTween:Play()
    end)
end

-- Connect button events
copyDiscordBtn.MouseButton1Click:Connect(copyDiscordLink)
minimizeBtn.MouseButton1Click:Connect(minimizeWindow)
closeBtn.MouseButton1Click:Connect(closeWindow)

-- Add hover effects
addHoverEffect(copyDiscordBtn, Color3.fromRGB(204, 0, 0), Color3.fromRGB(255, 0, 0))
addHoverEffect(minimizeBtn, Color3.fromRGB(255, 200, 50), Color3.fromRGB(255, 170, 0))
addHoverEffect(closeBtn, Color3.fromRGB(204, 0, 0), Color3.fromRGB(255, 0, 0))

-- Initialize
spawn(function()
    showLoading()
    createParticles()
    
    -- Entrance animation
    mainFrame.Size = UDim2.new(0, 0, 0, 0)
    mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
    
    local entranceTween = TweenService:Create(
        mainFrame,
        TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Size = UDim2.new(0, 500, 0, 450), Position = UDim2.new(0.5, -250, 0.5, -225)}
    )
    entranceTween:Play()
    
    showNotification("Script Status UI Loaded", true)
end)

-- Draggable window functionality
local dragging = false
local dragStart = nil
local startPos = nil

mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

mainFrame.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

print("Script Status UI loaded successfully!")
print("Discord: https://discord.gg/UMVSe5XfGC")
print("Created by XwareEXE")
